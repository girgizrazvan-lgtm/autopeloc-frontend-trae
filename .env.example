# Database
# Get from Supabase Dashboard → Settings → Database → Connection string
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres?sslmode=require

# Admin Credentials (OBLIGATORIU pentru securitate)
ADMIN_EMAIL=admin@yourdomain.ro

# IMPORTANT: Use ADMIN_PASSWORD_HASH for production (secure)
# Generate hash: node -e "const bcrypt = require('bcryptjs'); bcrypt.hash('your_password', 12, (e,h) => console.log(h))"
ADMIN_PASSWORD_HASH=$2a$12$your_bcrypt_hash_here

# DEPRECATED: ADMIN_PASSWORD (doar pentru dezvoltare locală sau migrare)
# Pentru securitate, folosește ADMIN_PASSWORD_HASH în producție
# ADMIN_PASSWORD=your_secure_password_here

# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://[YOUR-PROJECT-REF].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# Supabase Service Role Key (opțional - doar pentru operațiuni admin cu permisiuni complete)
# Get from Supabase Dashboard → Settings → API → service_role key (secret)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Site URL
NEXT_PUBLIC_SITE_URL=http://localhost:3001
# În producție: NEXT_PUBLIC_SITE_URL=https://yourdomain.ro

# Email (Resend)
RESEND_API_KEY=re_your_api_key_here
RESEND_FROM_EMAIL=noreply@yourdomain.ro

# Email notificări interne (opțional)
# Email-ul unde se primesc notificări pentru rezervări noi
INTERNAL_NOTIFICATION_EMAIL=contact@yourdomain.ro

# Google Search Console (opțional)
# Pentru verificarea site-ului în Google Search Console
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your_google_verification_code

# Admin API (opțional - doar dacă folosești integrare externă cu admin)
# NEXT_PUBLIC_ADMIN_URL=https://admin.yourdomain.ro
# ADMIN_API_KEY=your_admin_api_key_here

# Vercel Blob (pentru upload imagini)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_your_token_here
